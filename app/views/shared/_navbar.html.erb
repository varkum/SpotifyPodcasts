<nav class="w-full flex flex-col lg:flex-row flex-grow justify-between lg:items-center bg-zinc-700 p-6" data-controller="navbar">
    <div class="flex flex-grow lg:items-center justify-between" >
        <h1 class="font-bold text-2xl mr-8">
            <%= link_to "SpotifyPodcasts", home_path %>
        </h1>
        
        <div class="hidden lg:contents">
            <% if Current.user.present? %>
                <div class="flex flex-grow font-semibold">
                    <div class="mr-4">
                        <%= link_to "My Episodes", episodes_path %>
                    </div>

                    <div class="mr-4">
                        <%= link_to "Favorites", favorites_path %>
                    </div>
                    <div class="mr-4">
                        <%= link_to "History", history_path %>
                    </div>
                </div>
                <div>
                    <div data-action="click->navbar#toggleSignout" class="flex items-center border-green-600 border-2 rounded-3xl px-3 py-2 hover:opacity-70 hover:cursor-pointer font-semibold">
                        <img src="<%= Current.user.image %>" alt="profile" width="30" height="30" class="rounded-3xl mr-2"/>
                        <%= Current.user.name %>
                        <i data-navbar-target="icon" class="fa-solid fa-caret-down ml-2"></i>
                    </div>
                    <div data-navbar-target="menu" class="hidden fixed">
                        <%= button_to "Sign out", signout_path, method: :delete, class: "bg-red-500 rounded px-16 py-2 hover:opacity-70 hover:cursor-pointer" %>
                    </div>
                </div>
            <% end %>
        </div>

        
        <div class="lg:hidden">
            <button type="button" class="inline-flex items-center justify-center rounded-md p-2 text-gray-400 hover:bg-gray-700 hover:text-white focus:outline-none focus:ring-2 focus:ring-inset focus:ring-white" aria-controls="mobile-menu" aria-expanded="false">
            <span class="sr-only">Open main menu</span>
            <i class="fa-solid fa-bars fa-xl" id="open-btn" data-action="click->navbar#open"></i>
            </button>
        </div>
    </div>
    
    <% if Current.user.present? %>
        <div class="hidden lg:hidden" data-navbar-target="mobileMenu">
            <div class="flex flex-col font-light">
                <div class="mt-4">
                            <%= link_to "My Episodes", episodes_path %>
                        </div>

                        <div class="mt-4">
                            <%= link_to "Favorites", favorites_path %>
                        </div>
                        <div class="mt-4">
                            <%= link_to "History", history_path %>
                        </div>
                        <div>
                            <div data-action="click->navbar#toggleMobileSignout" class="flex items-center mt-6 py-2 hover:opacity-70 hover:cursor-pointer font-semibold">
                                <img src="<%= Current.user.image %>" alt="profile" width="30" height="30" class="rounded-3xl mr-2"/>
                                <%= Current.user.name %>
                                <i data-navbar-target="mobileIcon" class="fa-solid fa-caret-down ml-2"></i>
                            </div>
                            <div data-navbar-target="mobileSignout" class="hidden">
                                <%= button_to "Sign out", signout_path, method: :delete, class: "bg-red-500 rounded px-16 py-2 hover:opacity-70 hover:cursor-pointer", id:"mobile_signout" %>
                            </div>
                        </div>
            </div>
        </div>
    <% end %>
    
</nav>